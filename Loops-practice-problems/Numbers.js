var numbers = 0;
while (numbers < 20) {
    console.log(numbers);
    // numbers += 1;
    // numbers = numbers + 1;
    numbers++;
}

var number = 0;
while (number < 20) {
    console.log(number);
    // number++;
    number = number + 3;

}